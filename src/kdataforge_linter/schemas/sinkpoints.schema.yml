$schema: https://json-schema.org/draft/2020-12/schema
$id: https://kmods.dev/kdataforge-linter/schemas/sinkpoints.schema.yml
x-kdf-type: sinkpoints
title: KDataForge sink points
allOf:
  - $ref: https://kmods.dev/kdataforge-linter/schemas/common.schema.yml#/$defs/documentBase
  - type: object
    required: [entries]
    properties:
      type: { const: sinkpoints }
      track: { enum: [Default, Exploration], default: Default }
      entries:
        type: array
        minItems: 1
        items:
          type: object
          additionalProperties: false
          required: [item, points]
          properties:
            item:
              $ref: https://kmods.dev/kdataforge-linter/schemas/common.schema.yml#/$defs/nonEmptyString
            points: { type: integer }
            overridden: { type: integer }
unevaluatedProperties: false

